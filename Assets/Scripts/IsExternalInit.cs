namespace System.Runtime.CompilerServices
{
    public class IsExternalInit
    {
        // This class is used to support C# 9.0's init-only properties in Unity, which does not support it natively.
        // It is a workaround to allow init-only properties to be used in Unity scripts.
        // The class itself does not contain any members or methods.
    }
}